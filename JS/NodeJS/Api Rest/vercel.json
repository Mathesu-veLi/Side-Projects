{
  "rewrites": [{ "source": "/(.*)", "destination": "/dist" }],
  "version": 2,
    "name": "api-rest",
    "builds": [
        {
            "src": "app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/dist/(.*)",
            "dest": "app.js"
        }
    ]
}
